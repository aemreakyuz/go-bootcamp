package main

func main() {
	/*
		CONVERT
	*/

	/* 	var myString = "1"
	   	var x = 10
	   	var f float32 = 2.0

	   	fmt.Println(myString, x, f)
	   	number, _ := strconv.Atoi(myString) //atoi --> string den integer a   //itoa integer dan string e

	   	fmt.Println(number)
	   	x = number

	   	fmt.Println(x)
	   	fmt.Printf("%T", f)

	   	/*
	   		CASTING

	   	//The expression T(v) converts the value v to the type T
	   	//Some numeric conversions;

	   	var i int = 42
	   	var f float64 = float64(i)
	   	var u uint = uint(f)

	   	//veya daha basit;

	   	i := 42
	   	f := float64(i)
	   	u := uint(f)

	   	var x, y int = 3, 4
	   	var f float64 = math.Sqrt(float64(x*x + y*y))
	   	var z uint = uint(f)
	   	fmt.Println(x, y, z) */

}
